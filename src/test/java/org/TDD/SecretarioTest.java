package org.TDD;

import org.TDD.entity.Usuario;
import org.TDD.interfaces.iUsuario;
import org.junit.jupiter.api.Test;

import static org.junit.Assert.*;

public class SecretarioTest {

    public SecretarioTest() {}

    @Test
    public void secretarioCriaSalaTest() {
        int bloco = 2;
        String anda = "Superior";
        int numero = 2;
        boolean projetor = false;
        boolean arCondicionado = true;
        int capacidade = 40;

        //Sala sala = new sala(bloco, andar, numero, projeto, arCondicionado, capacidade);

        //assertEquals(getSalaById(sala.getId), sala);
    }

}
